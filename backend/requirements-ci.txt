# Slim dependency set for CI test runs.
# Heavy runtime deps (torch, diffusers, transformers, manim, chromadb) are
# imported lazily by the app and gracefully degrade when absent, so tests
# run without them.
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
networkx>=3.0
email-validator>=2.0.0
plotly>=5.0.0
sympy>=1.12
Pillow>=10.0.0
pytesseract>=0.3.10
matplotlib>=3.8
numpy
