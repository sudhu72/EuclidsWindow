.venv/
__pycache__/
.env
node_modules/
*.sqlite
*.db
.DS_Store
backend/static/animations/
# Runtime settings, including saved cloud API keys. Docker keeps this on a
# named volume, but running the app directly on the host writes it here.
backend/data/state/
